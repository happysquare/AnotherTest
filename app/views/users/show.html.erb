<h1>
<%=gravator_for(@user) %>
<%= @name %>
</h1>
<% unless @microposts.empty? %>
	<%= will_paginate @microposts %>
	<table class="microposts">
		<%= render @microposts %>
	</table>
	<%= will_paginate( @microposts )%>
<%end%>