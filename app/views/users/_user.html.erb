<li>
	<%= gravator_for user, :size=> 25 %>
	<%= link_to user.name, user %>
	<% if current_user.admin? %>
		<%= link_to "delete",user, :method => :delete, :confirm => "Are you sure you want to delete this user?", :title => "Delete #{user.name}" %>
	<%end%>
</li>