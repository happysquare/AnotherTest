<% if object.errors.any? %>
	<div id="error_explanation">
		<p>
			<%=pluralize(object.errors.count,"error") %> prevented this form from being saved</p>
		</p>
		<ul>
		<% object.errors.full_messages.each do |e| %>
			<li><%= e %></li>
		<%end%>	
		</ul>
	</div>
<%end%>